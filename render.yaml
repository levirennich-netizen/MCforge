services:
  - type: web
    name: mcforge-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    envVars:
      - key: XAI_API_KEY
        sync: false
      - key: POLLINATIONS_API_KEY
        sync: false
      - key: CORS_ORIGINS
        value: '["*"]'
      - key: DATA_DIR
        value: /app/data
      - key: PORT
        value: "8000"
